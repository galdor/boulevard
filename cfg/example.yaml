---
logger:
  backend_type: "terminal"
  terminal_backend:
    color: true
  debug_level: 1

---
module: "http-server"
name: "http"
data:
  listeners:
    - address: ":8080"
    - address: ":4430"
      tls:
        domains: ["localhost", "www.localhost"]
  handlers:
    - path: "/boulevard/"
      serve:
        path: "."
    - path: "/forbidden/"
      reply:
        status: 403
        body: "access denied\n"

---
module: "tcp-server"
name: "tcp"
data: {}
